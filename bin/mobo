#!/usr/bin/env ruby
require_relative '../mobo'

MOBO_DEVICES_FILE = Dir.pwd + '/.mobo-devices.yaml'

case ARGV[0]
when "up"
  MOBO.data = YAML::load_file(Dir.pwd + '/devices.yaml')
  MOBO.up
when "status"
  MOBO.data = YAML::load_file(MOBO_DEVICES_FILE)
  MOBO.status
when "destroy"
  MOBO.data = YAML::load_file(MOBO_DEVICES_FILE)
  MOBO.destroy
end